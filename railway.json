{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python ai3+5自动预测.py",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/health"
  }
}
